pkg info xorg-server > /dev/null 2>&1
_HAS_XORG=$?
pkg info wayland > /dev/null 2>&1
_HAS_WAYLAND=$?
if [ $_HAS_XORG -ne 0 ] && [ $_HAS_WAYLAND -ne 0 ]; then
    _WARN "disabling python-detect-secrets as no graphical environment was detected"
    _FEATURE_PYTHON_DETECT_SECRETS_DISABLED=1
fi
